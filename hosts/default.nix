{ ... }:
{
  # Fallback profile for new/unknown hosts.
  # Use as starting point for new machines:
  #   1. Add hosts/<hostname>.nix with overrides
  #   2. Run: HOSTNAME=<hostname> darwin-rebuild switch --flake . --impure
}
